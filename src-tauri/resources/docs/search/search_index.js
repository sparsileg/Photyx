var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"index.html","title":"Photyx Help","text":"<p>Welcome to the Photyx documentation.</p> <p>Astrophotography sessions produce a lot of raw subs, and not all of them are worth keeping. Photyx quickly sorts the good frames from the bad \u2014 flagging the ones degraded by poor tracking, bad focus, or passing clouds \u2014 so only your best subs move on to PixInsight, Siril, or whatever you use for final processing. It also gives you a fast quick-look stack along the way, so you can confirm a session was worth the trouble before you commit to full processing.</p> <p>Use the tutorials below to get started, or jump straight to the Command Reference if you already know what you're looking for.</p>"},{"location":"index.html#tutorials","title":"Tutorials","text":"<p>New to Photyx? Start here, in order:</p> <ul> <li>Your First Session \u2014 load a    session, review frames, run quality analysis, and produce a    quick-look stack, start to finish.</li> <li>Automating with pcode \u2014 turn the    workflow from Tutorial 1 into a reusable, scripted macro.</li> <li>Macros and Multiple Sessions \u2014    combine several imaging nights of the same target into a single    analysis-and-stack pass.</li> </ul>"},{"location":"index.html#command-reference","title":"Command Reference","text":"<ul> <li>pcode Command Reference \u2014 every pcode    command, its arguments, and worked examples.</li> </ul>"},{"location":"1-your-first-session.html","title":"Tutorial 1: Your First Session","text":"<p>What you'll learn: how to load a session, review frames, run quality analysis, reject the bad ones, and produce a quick-look stack \u2014 the core Photyx workflow, start to finish.</p> <p>Before you begin: you'll need a folder of your own FITS light frames from a real imaging session. This tutorial will modify those files \u2014 it adds a <code>FILTER</code> keyword to every frame partway through, and removes it again at the end. If you'd rather not modify your files at all, make a copy of the session folder first and work from the copy.</p>"},{"location":"1-your-first-session.html#1-launch-photyx","title":"1. Launch Photyx","text":"<p>When Photyx opens you'll see:</p> <ul> <li>Menu Bar across the top (File, Session, Edit, View, Analyze, Tools, Help)</li> <li>Toolbar below it, with viewer controls and a file/directory count</li> <li>Icon Sidebar on the left \u2014 File Browser, Keyword Editor, Macro   Library, and Plugin Manager panels live behind these icons</li> <li>Viewer filling the center \u2014 this is where your images display</li> <li>Status Bar along the bottom, which shows what Photyx is doing</li> </ul> <p>Nothing is loaded yet, so the toolbar's file count is empty. That changes in the next step.</p>"},{"location":"1-your-first-session.html#2-add-files","title":"2. Add Files","text":"<p>Load your session two different ways, so you've seen both:</p> <ul> <li>File browser: <code>Session &gt; Add Files\u2026</code> (or Ctrl+O), then select   roughly half of your frames.</li> <li>Drag-and-drop: drag the remaining files directly from your   operating system's file browser into the Photyx window to add them   to the same session.</li> </ul> <p>Either way, watch the toolbar \u2014 it updates to show <code>N files \u00b7 M directories</code> once your frames are loaded.</p> <p>Note: Photyx works best with FITS (<code>.fit</code>/<code>.fits</code>/<code>.fts</code>) files. XISF is also fully supported if that's what your capture software produces.</p>"},{"location":"1-your-first-session.html#3-view-a-frame","title":"3. View a Frame","text":"<p>Click through a few frames using the viewer's navigation controls. Try zooming in on a star field. Then apply AutoStretch to bring out detail in an otherwise flat-looking linear image \u2014 this only changes the display, not your underlying pixel data.</p>"},{"location":"1-your-first-session.html#4-blink-for-a-quick-review","title":"4. Blink for a Quick Review","text":"<p>Open the Blink tab and press play. Blink rapidly cycles through your frames at reduced resolution so you can eyeball focus, tracking, and cloud drift across the whole session in seconds \u2014 well before running any formal analysis. Try stepping frame-by-frame as well as continuous playback, and adjust the Min Delay if it's cycling too fast or slow to follow.</p>"},{"location":"1-your-first-session.html#5-tag-the-session-with-a-keyword","title":"5. Tag the Session with a Keyword","text":"<p>The Keyword Editor panel is great for looking at one frame's header, but it can only edit one file at a time. To add a keyword across an entire session at once, use the console instead.</p> <p>Open the console (the collapsible panel at the bottom of the viewer) and run:</p> <pre><code>AddKeyword name=FILTER value=duo comment=\"Tutorial test tag\" scope=all\nWriteCurrent\n</code></pre> <p><code>scope=all</code> applies the keyword to every loaded frame in one command. <code>WriteCurrent</code> writes the change back to your files \u2014 for FITS files this only rewrites header keywords, the pixel data on disk is untouched.</p> <p>This keyword isn't required by anything downstream in this tutorial \u2014 it's here so you get comfortable with the console before Tutorial 2. We'll remove it again in the final step.</p>"},{"location":"1-your-first-session.html#6-run-analyze-frames","title":"6. Run Analyze Frames","text":"<p>Open <code>Analyze &gt; Analyze Frames</code>. You'll be prompted to pick a threshold profile \u2014 choose Default for this run and confirm.</p> <p>Photyx computes four quality metrics per frame \u2014 background median, FWHM, eccentricity, and star count \u2014 and classifies every frame as PASS or REJECT relative to the rest of this session. A frame that would pass in bright moonlight might reject in a darker session, and that's intentional: classification is always session-relative, never an absolute standard.</p>"},{"location":"1-your-first-session.html#7-read-analysis-results","title":"7. Read Analysis Results","text":"<p>Open <code>Analyze &gt; Analysis Results</code>. You'll see a sortable table: filename, the four metrics, PXFLAG (PASS/REJECT), and a rejection category.</p> <p>Category badges tell you why a frame was rejected:</p> Badge Meaning Triggered by O (red) Optical FWHM and/or eccentricity T (yellow) Transparency Low star count B (blue) Sky Brightness High background median <p>A frame can show more than one badge if more than one metric fired.</p>"},{"location":"1-your-first-session.html#8-read-analysis-graph","title":"8. Read Analysis Graph","text":"<p>Open <code>Analyze &gt; Analysis Graph</code> for the same results, visually. Each dot is a frame \u2014 white for PASS, colored per the table above for REJECT \u2014 plotted against sigma bands with the reject threshold lines drawn in. Click any dot to jump straight to that frame in the viewer.</p>"},{"location":"1-your-first-session.html#9-toggle-a-flag","title":"9. Toggle a Flag","text":"<p>Right-click any row in Analysis Results. If it's a REJECT, you'll see \"Set to PASS\" \u2014 if it's a PASS, \"Set to REJECT.\" Try toggling one.</p> <p>Nothing is written to disk yet \u2014 this is a local, reversible override that only takes effect when you commit.</p>"},{"location":"1-your-first-session.html#10-commit-results","title":"10. Commit Results","text":"<p>Click Commit Results. Here's exactly what happens:</p> <ul> <li>Any toggle you made in step 9 is applied first</li> <li>Every frame still flagged REJECT is moved into a <code>rejected/</code>   subfolder inside its own source directory, with <code>.reject</code> appended   to the filename (e.g. <code>frame001.fit.reject</code>)</li> <li>This is a move, not a delete \u2014 nothing is destroyed</li> <li>PASS frames stay loaded and the session stays open, ready for the   next step</li> </ul>"},{"location":"1-your-first-session.html#11-stack-the-surviving-frames","title":"11. Stack the Surviving Frames","text":"<p>Open the Stacking Workspace (<code>Analyze</code> menu) and run a stack on your remaining PASS frames.</p> <p>At a high level, Photyx groups frames by imaging session/rotation, aligns each frame to a reference using star matching, and combines them with a sigma-clipped mean \u2014 outlier pixels (satellite trails, cosmic ray hits) are automatically excluded from the final result on a per-pixel basis.</p> <p>Reviewer note for Stan: I don't have exact button/control labels for the Stacking Workspace UI in the reference docs \u2014 please correct this step with the real control names before publishing.</p>"},{"location":"1-your-first-session.html#12-view-and-export-the-stack","title":"12. View and Export the Stack","text":"<p>Preview the stretched result in the Stacking Workspace. When you're happy with it, export it from the console:</p> <pre><code>WriteXISF destination=\"&lt;your output folder&gt;\" stack=true\nPrint \"Stack written to: \" + $STACKED\n</code></pre>"},{"location":"1-your-first-session.html#13-clean-up-the-test-keyword","title":"13. Clean Up the Test Keyword","text":"<p>Back in the console, remove the <code>FILTER</code> keyword you added in step 5:</p> <pre><code>DeleteKeyword name=FILTER scope=all\nWriteCurrent\n</code></pre> <p>Your files are now back to their original header state.</p>"},{"location":"1-your-first-session.html#14-wrap-up","title":"14. Wrap-Up","text":"<p>What you just produced is a quick-look validation stack \u2014 good enough to confirm your data is worth carrying forward, not a finished image. From here, take your surviving (PASS) frames into PixInsight or Siril for real processing: calibration, deeper registration, and proper stretching.</p> <p>Everything you just did by clicking can also be done \u2014 and automated \u2014 with a script. That's Tutorial 2: Automating with pcode.</p>"},{"location":"2-automating-with-pcode.html","title":"Tutorial 2: Automating with pcode","text":"<p>What you'll learn: how to turn the workflow from Tutorial 1 into a reusable script, using pcode \u2014 Photyx's built-in macro language.</p> <p>Before you begin: complete Tutorial 1 first. This tutorial assumes you're comfortable with what each step does; here we're just learning to write it instead of clicking it.</p>"},{"location":"2-automating-with-pcode.html#1-open-the-console","title":"1. Open the Console","text":"<p>The console lives as a collapsible panel at the bottom of the viewer. Click its header to expand it to a larger overlay if you want more room to read output.</p> <p>Notice the Trace / No Trace toggle. In Trace mode, every command you run is echoed back with its fully resolved arguments before it executes \u2014 useful while you're learning, since you can see exactly what Photyx received. Turn it on for this tutorial.</p>"},{"location":"2-automating-with-pcode.html#2-run-your-first-command","title":"2. Run Your First Command","text":"<p>Type this into the console and press Enter:</p> <pre><code>CountFiles\n</code></pre> <p><code>CountFiles</code> counts the frames currently loaded and stores the result in a variable called <code>$filecount</code>. Every pcode command follows this same shape: a command name, followed by zero or more <code>name=value</code> arguments. <code>CountFiles</code> happens to take none.</p> <p>Try:</p> <pre><code>Print $filecount\n</code></pre>"},{"location":"2-automating-with-pcode.html#3-reproduce-tutorial-1-as-a-script","title":"3. Reproduce Tutorial 1 as a Script","text":"<p>Everything you did by hand in Tutorial 1 can be typed as one block:</p> <pre><code>ClearSession\nAddFiles paths=\"&lt;your session directory&gt;/*.fit\"\n\nAddKeyword name=FILTER value=duo comment=\"Tutorial test tag\" scope=all\nWriteCurrent\n\nAnalyzeFrames profile=\"Default\"\nShowAnalysisResults\n</code></pre> <p>Run it, review the results the same way you did in Tutorial 1, then continue the same script to commit and stack:</p> <pre><code>CommitAnalysis append=.reject\nStackFrames\n</code></pre> <p>Notice <code>CommitAnalysis</code> is the scripted equivalent of clicking Commit Results, and it takes the same <code>.reject</code> suffix you saw appear on rejected filenames in Tutorial 1.</p>"},{"location":"2-automating-with-pcode.html#4-introduce-variables","title":"4. Introduce Variables","text":"<p>Variables are set with <code>Set</code> and read back with a <code>$</code> prefix:</p> <pre><code>Set targetName = \"M31\"\nSet label = \"Session: \" + $targetName\nPrint $label\n</code></pre> <p>Note the double quotes around the string literal \u2014 <code>Set</code>'s right-hand side requires them for any text value. The <code>+</code> operator concatenates when either side isn't purely numeric, so <code>$targetName</code> slots naturally into the larger string.</p>"},{"location":"2-automating-with-pcode.html#5-introduce-a-loop","title":"5. Introduce a Loop","text":"<p>Here's a per-frame FWHM report \u2014 measuring focus quality frame by frame and printing each result:</p> <pre><code>ReadImages path=\"&lt;your session directory&gt;\"\n\nCountFiles\nFor i = 0 To $filecount - 1\n  SetFrame index=$i\n  GetKeyword name=DATE-OBS\n  ComputeFWHM\n  Print $DATE-OBS + \"  FWHM=\" + $fwhm\nEndFor\n\nLog path=\"&lt;your log folder&gt;/fwhm_report.log\"\n</code></pre> <p><code>For i = 0 To $filecount - 1</code> steps through every loaded frame by index. <code>SetFrame</code> makes frame <code>i</code> the active one, so every command after it (<code>GetKeyword</code>, <code>ComputeFWHM</code>) operates on that specific frame. <code>Log</code> writes everything printed since the last <code>Log</code> call out to a file \u2014 handy for keeping a record of a batch run.</p>"},{"location":"2-automating-with-pcode.html#6-introduce-a-conditional","title":"6. Introduce a Conditional","text":"<p>Let's flag frames with poor focus. Inside the same loop, right after <code>ComputeFWHM</code>:</p> <pre><code>If $fwhm &gt; 3.0\n  Print \"Poor focus \u2014 skipping\"\nElse\n  Print \"Focus acceptable\"\nEndIf\n</code></pre> <p><code>If</code>/<code>Else</code>/<code>EndIf</code> blocks can nest, and comparisons use <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code> \u2014 a single <code>=</code> is reserved for <code>Set</code> and isn't a valid condition operator.</p>"},{"location":"2-automating-with-pcode.html#7-save-it-as-a-macro","title":"7. Save It as a Macro","text":"<p>Open the Macro Editor from the Icon Sidebar. Paste in your FWHM report script (with the conditional from step 6 added), give it a name \u2014 for example <code>fwhm-report</code> \u2014 and save it. Saving keeps a version history, so you can always roll back to an earlier draft later.</p>"},{"location":"2-automating-with-pcode.html#8-run-it-two-more-ways","title":"8. Run It Two More Ways","text":"<p>You've been running commands directly in the console. Now try the other two ways to run a saved macro:</p> <ul> <li>From the Macro Library \u2014 open the panel, find <code>fwhm-report</code>, and   run it from there.</li> <li>Via <code>RunMacro</code> \u2014 type this into the console:</li> </ul> <pre><code>RunMacro name=\"fwhm-report\"\n</code></pre> <p><code>RunMacro</code> is also how one macro calls another \u2014 useful once you   start building a library of small, reusable scripts.</p>"},{"location":"2-automating-with-pcode.html#9-pin-it-to-quick-launch","title":"9. Pin It to Quick Launch","text":"<p>Back in the Macro Library, pin <code>fwhm-report</code> to the Quick Launch bar. It now appears as a one-click button above the viewer \u2014 no console or macro library needed to run it next time. Pin as many macros as you like; buttons wrap to a new row automatically.</p>"},{"location":"2-automating-with-pcode.html#10-wrap-up","title":"10. Wrap-Up","text":"<p>You've now written variables, a loop, a conditional, and a saved, pinnable macro \u2014 the building blocks for automating almost anything in Photyx. From here, the pcode Scripting Guide is your ongoing reference: every command, its arguments, and more worked examples than we could fit in this tutorial.</p> <p>Next up, if you're managing more than one imaging session for the same target: Tutorial 3: Multi-Session Project Workflow.</p>"},{"location":"3-multi-session-project.html","title":"Tutorial 3: Multi-Session Project Workflow","text":"<p>What you'll learn: how to analyze and stack frames from multiple imaging nights of the same target as a single combined pool, using pcode.</p> <p>Before you begin: complete Tutorials 1 and 2 first. This tutorial is pcode-only \u2014 everything here is written as a script, with no UI-only equivalent shown.</p>"},{"location":"3-multi-session-project.html#1-the-problem","title":"1. The Problem","text":"<p>You've imaged the same target across several nights. Each night is its own session folder, with its own conditions \u2014 one night might be clearer than another, seeing might vary, and so on. Tutorial 1 walked through analyzing one session. This tutorial combines all of them into a single pool before analyzing and stacking \u2014 different frames compete against each other for PASS/REJECT purposes, not just against their own night.</p>"},{"location":"3-multi-session-project.html#2-the-folder-hierarchy-this-tutorial-assumes","title":"2. The Folder Hierarchy This Tutorial Assumes","text":"<p>This tutorial assumes a project laid out as one parent folder containing one subfolder per imaging session, each with its own <code>lights/</code> folder inside:</p> <pre><code>M82/\n\u251c\u2500\u2500 M82-ircut-sess-01/\n\u2502   \u2514\u2500\u2500 lights/\n\u2502       \u251c\u2500\u2500 frame001.fit\n\u2502       \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 M82-ircut-sess-02/\n\u2502   \u2514\u2500\u2500 lights/\n\u2502       \u2514\u2500\u2500 ...\n\u2514\u2500\u2500 M82-ircut-sess-03/\n    \u2514\u2500\u2500 lights/\n        \u2514\u2500\u2500 ...\n</code></pre> <p>If your own folder structure looks different, adjust the glob pattern in step 4 to match.</p>"},{"location":"3-multi-session-project.html#3-set-a-project-variable","title":"3. Set a Project Variable","text":"<p>Rather than repeating the full project path in every command, set it once at the top of the script:</p> <pre><code>Set project = \"J:/projects/M82\"\n# Linux/Mac equivalent: Set project = \"/data/projects/M82\"\n</code></pre> <p>Every path from here on can reference <code>$project</code> instead of the full string.</p>"},{"location":"3-multi-session-project.html#4-load-all-sessions-with-one-glob","title":"4. Load All Sessions with One Glob","text":"<p><code>AddFiles</code> accepts glob patterns, including wildcards in the middle of a path \u2014 not just at the end. This lets one command pull frames from every session folder in the project at once:</p> <pre><code>ClearSession\nAddFiles paths=\"$project/M82-*-sess-*/lights/*.fit\"\n\nCountFiles\nAssert expression=\"$filecount &gt; 0\"\nPrint \"Loaded \" + $filecount + \" frames across all sessions\"\n</code></pre> <p><code>Assert</code> halts the script with a clear error if the glob matched nothing \u2014 worth having in any script that depends on files actually being found, rather than silently continuing with zero frames.</p>"},{"location":"3-multi-session-project.html#5-run-analyze-frames-on-the-combined-pool","title":"5. Run Analyze Frames on the Combined Pool","text":"<pre><code>AnalyzeFrames profile=\"Project\"\n</code></pre> <p>This is the same <code>AnalyzeFrames</code> command from Tutorials 1 and 2 \u2014 the difference is entirely in what's loaded. Because every frame from every session is in the session pool together, PASS/REJECT is decided relative to the combined population, not any single night.</p> <p>Use (or create) a threshold profile named to reflect this run \u2014 the <code>\"Project\"</code> name here is just an example; name your own profile however makes sense for your workflow.</p>"},{"location":"3-multi-session-project.html#6-review-results","title":"6. Review Results","text":"<pre><code>ShowAnalysisResults\n</code></pre> <p>This opens the same Analysis Results table from Tutorial 1 \u2014 sort it, inspect category badges, and confirm the classifications make sense before committing. Nothing has been written to disk yet.</p>"},{"location":"3-multi-session-project.html#7-commit-results","title":"7. Commit Results","text":"<pre><code>CommitAnalysis append=.project\n</code></pre> <p>Just like Tutorial 1, REJECT frames move to a <code>rejected/</code> subfolder within their own original session directory (not a single combined rejected folder) \u2014 each frame's rejection travels back to where it came from. The <code>.project</code> suffix on the moved filename distinguishes this pass from any other rejection pass you might run later.</p>"},{"location":"3-multi-session-project.html#8-stack-the-combined-set","title":"8. Stack the Combined Set","text":"<pre><code>StackFrames\n</code></pre> <p>All surviving frames \u2014 regardless of which night they came from \u2014 are aligned and combined into one stack. Photyx's rotational grouping handles frames from different nights automatically, so you don't need to do anything special here even if your equipment was rotated or re-polar-aligned between sessions.</p>"},{"location":"3-multi-session-project.html#9-stretch-and-commit-the-stretch","title":"9. Stretch and Commit the Stretch","text":"<pre><code>CommitStretch shadow_clip=-3.5 target_bg=0.10\n</code></pre> <p>Unlike Tutorial 1's default preview stretch, here we're specifying exact values \u2014 useful once you have a stretch you like and want a repeatable result rather than an auto-computed one each time.</p>"},{"location":"3-multi-session-project.html#10-write-the-result","title":"10. Write the Result","text":"<pre><code>WriteXISF destination=\"$project/stacked\" stack=true\nPrint \"Stack complete: \" + $STACKED\n</code></pre>"},{"location":"3-multi-session-project.html#11-wrap-up","title":"11. Wrap-Up","text":"<p>This workflow is worth the extra setup specifically when you have multiple nights of the same target and want them evaluated and combined together. If you're only ever working with a single session at a time, Tutorial 1's simpler workflow \u2014 run once per session \u2014 is all you need, and is a good default until a project genuinely spans more than one night.</p>"},{"location":"reference/index.html","title":"pcode Command Reference","text":"<p>pcode is Photyx's built-in scripting language \u2014 line-oriented, with variables, conditionals, loops, and commands covering session management, file I/O, keywords, analysis, and stacking.</p> <p>New to pcode? Tutorial 2: Automating with pcode is the place to start. This section is the full reference to come back to afterward.</p>"},{"location":"reference/index.html#sections","title":"Sections","text":"<ul> <li>Language Basics \u2014 syntax, variables,   arithmetic, flow control, error handling, console output, trace mode</li> <li>Session Commands \u2014 <code>AddFiles</code>,   <code>ReadImages</code>, <code>ClearSession</code>, <code>LoadFile</code>, <code>CountFiles</code>,   <code>FilterByKeyword</code>, <code>RejectFrame</code></li> <li>Write &amp; Export Commands \u2014   <code>WriteCurrent</code>, <code>WriteFrame</code>, <code>WriteFIT</code>, <code>WriteXISF</code>, <code>CopyFile</code>,   <code>MoveFile</code></li> <li>Keyword Commands \u2014 <code>AddKeyword</code>,   <code>ModifyKeyword</code>, <code>DeleteKeyword</code>, <code>CopyKeyword</code>, <code>GetKeyword</code>,   <code>ListKeywords</code></li> <li>Analysis Commands \u2014 <code>AnalyzeFrames</code>,   <code>CommitAnalysis</code>, <code>ExportAnalysisReport</code>, <code>ComputeFWHM</code>,   <code>ComputeEccentricity</code>, <code>CountStars</code>, <code>GetHistogram</code>,   <code>ContourHeatmap</code>, <code>BackgroundMedian</code></li> <li>Image Processing \u2014 <code>AutoStretch</code>,   <code>DebayerImage</code></li> <li>Stacking Commands \u2014 <code>StackFrames</code>,   <code>CommitStretch</code>, <code>ClearStack</code></li> <li>Display &amp; Navigation \u2014 <code>SetFrame</code>,   <code>CacheFrames</code>, <code>ClearAnnotations</code>, <code>ShowAnalysisGraph</code>,   <code>ShowAnalysisResults</code></li> <li>Scripting Utilities \u2014 <code>Set</code>, <code>Print</code>,   <code>Assert</code>, <code>CountMatches</code>, <code>GetSystemPath</code>, <code>RunMacro</code>, <code>Log</code>,   <code>If</code>/<code>Else</code>/<code>EndIf</code>, <code>For</code>/<code>EndFor</code>, Console Built-ins</li> <li>Complete Examples \u2014 worked, end-to-end scripts</li> </ul>"},{"location":"reference/analysis-commands.html","title":"Analysis Commands","text":"<p>Commands for computing per-frame quality metrics, classifying frames as PASS/REJECT, and exporting or committing the results.</p>"},{"location":"reference/analysis-commands.html#analyzeframes","title":"<code>AnalyzeFrames</code>","text":"<p>Computes four quality metrics for loaded frames (FWHM, eccentricity, star count, background median) and classifies each frame as PASS or REJECT using iterative sigma clipping against session statistics.</p> <pre><code>AnalyzeFrames [profile=&lt;string&gt;] [scope=all|current] [threshold=&lt;float&gt;] [saturation=&lt;float&gt;]\n</code></pre> Argument Required Default Description <code>profile</code> No Threshold profile name to use for this run. If omitted, uses the active profile set in Edit &gt; Analysis Parameters. The active profile is not permanently changed. <code>scope</code> No <code>all</code> <code>all</code> runs the full two-pass session analysis (session stats, PASS/REJECT classification, reference-frame selection). <code>current</code> runs the same five metrics on only the current frame and prints raw values \u2014 no session stats or classification. <code>threshold</code> No <code>5.0</code> Star detection threshold in units of background std dev <code>saturation</code> No <code>0.98</code> Saturation threshold \u2014 stars at or above this value are rejected from detection <pre><code>AnalyzeFrames\nAnalyzeFrames profile=\"Session\"\nAnalyzeFrames profile=\"Project\"\nAnalyzeFrames scope=current\n</code></pre> <p>Results are visible in the Analysis Results and Analysis Graph views. See <code>ShowAnalysisGraph</code> and <code>ShowAnalysisResults</code>.</p>"},{"location":"reference/analysis-commands.html#commitanalysis","title":"<code>CommitAnalysis</code>","text":"<p>Moves all REJECT frames to a <code>rejected/</code> subfolder within each frame's source directory and removes them from the session. Pass frames remain loaded. Optionally appends a suffix to each moved filename.</p> <pre><code>CommitAnalysis [append=&lt;ext&gt;]\n</code></pre> Argument Required Default Description <code>append</code> No Suffix appended after the original filename extension (e.g. <code>append=.session</code> \u2192 <code>frame.fit.session</code>). Leading dot is optional. Defaults to no suffix. <pre><code>CommitAnalysis\nCommitAnalysis append=.session\n</code></pre>"},{"location":"reference/analysis-commands.html#exportanalysisreport","title":"<code>ExportAnalysisReport</code>","text":"<p>Exports the current analysis results as a Photyx session JSON file. If <code>path</code> is omitted, a filename is derived from the first frame and written to the system Downloads folder.</p> <pre><code>ExportAnalysisReport [path=&lt;path&gt;]\n</code></pre> Argument Required Description <code>path</code> No Full destination path for the JSON file. If omitted, written to the Downloads folder with an auto-derived filename. <pre><code>ExportAnalysisReport\nExportAnalysisReport path=\"D:/projects/M64/M64_sess_20241112_analysis.json\"\n</code></pre>"},{"location":"reference/analysis-commands.html#computefwhm","title":"<code>ComputeFWHM</code>","text":"<p>Computes the median Full Width at Half Maximum for detected stars in the current frame, reported in pixels (and arcseconds when <code>FOCALLEN</code>, <code>INSTRUME</code>, and <code>XBINNING</code> keywords are present) and displays per-star circle annotations on the viewer overlay.</p> <pre><code>ComputeFWHM [threshold=&lt;float&gt;] [peak_radius=&lt;int&gt;] [saturation=&lt;float&gt;]\n</code></pre> Argument Required Default Description <code>threshold</code> No <code>5.0</code> Star detection threshold in units of background std dev <code>peak_radius</code> No <code>3</code> Radius in pixels for the local-maximum test <code>saturation</code> No <code>0.98</code> Stars at or above this peak value are rejected as saturated <p>Side effect: Stores mean FWHM in <code>$fwhm</code>.</p> <pre><code>ComputeFWHM\nPrint $fwhm\n</code></pre>"},{"location":"reference/analysis-commands.html#computeeccentricity","title":"<code>ComputeEccentricity</code>","text":"<p>Computes mean star eccentricity for the current frame. Values near 0 = round stars; values near 1 = elongated stars.</p> <pre><code>ComputeEccentricity [threshold=&lt;float&gt;] [peak_radius=&lt;int&gt;] [saturation=&lt;float&gt;]\n</code></pre> Argument Required Default Description <code>threshold</code> No <code>5.0</code> Star detection threshold in units of background std dev <code>peak_radius</code> No <code>3</code> Radius in pixels for the local-maximum test <code>saturation</code> No <code>0.98</code> Stars at or above this peak value are rejected as saturated <p>Side effect: Stores result in <code>$eccentricity</code>.</p> <pre><code>ComputeEccentricity\nPrint $eccentricity\n</code></pre>"},{"location":"reference/analysis-commands.html#countstars","title":"<code>CountStars</code>","text":"<p>Counts the number of detected stars in the current frame using peak-finding on a sigma-clipped, background-subtracted image.</p> <pre><code>CountStars [threshold=&lt;float&gt;] [peak_radius=&lt;int&gt;] [flood_threshold=&lt;float&gt;] [saturation=&lt;float&gt;] [sigma=&lt;float&gt;] [iterations=&lt;int&gt;]\n</code></pre> Argument Required Default Description <code>threshold</code> No <code>5.0</code> Detection threshold in units of background std dev <code>peak_radius</code> No <code>3</code> Radius in pixels for the local-maximum test <code>flood_threshold</code> No <code>2.0</code> Flood-fill lower bound in units of background std dev <code>saturation</code> No <code>0.98</code> Stars at or above this peak value are rejected as saturated <code>sigma</code> No <code>3.0</code> Sigma-clipping threshold used for background estimation <code>iterations</code> No <code>5</code> Maximum sigma-clipping iterations for background estimation <p>Side effect: Stores result in <code>$starcount</code>.</p> <pre><code>CountStars\nPrint $starcount\n</code></pre>"},{"location":"reference/analysis-commands.html#gethistogram","title":"<code>GetHistogram</code>","text":"<p>Computes the histogram and basic statistics (median, std dev, clipping %) for the current frame. RGB frames get per-channel statistics.</p> <pre><code>GetHistogram\n</code></pre>"},{"location":"reference/analysis-commands.html#contourheatmap","title":"<code>ContourHeatmap</code>","text":"<p>Generates a false-color spatial FWHM heatmap for the current frame: stars are detected, per-star FWHM is measured, values are interpolated across an adaptive grid, and the result is rendered with contour lines. Writes the result as an XISF file named <code>&lt;source_stem&gt;_heatmap.xisf</code> in the source file's directory.</p> <pre><code>ContourHeatmap [palette=viridis|plasma|coolwarm] [contour_levels=&lt;int&gt;] [threshold=&lt;float&gt;] [saturation=&lt;float&gt;]\n</code></pre> Argument Required Default Description <code>palette</code> No <code>viridis</code> Color palette <code>contour_levels</code> No <code>10</code> Number of contour levels (minimum 2) <code>threshold</code> No <code>5.0</code> Star detection threshold in units of background std dev <code>saturation</code> No <code>0.98</code> Stars at or above this peak value are rejected as saturated <p>Side effect: Stores output file path in <code>$NEW_FILE</code>.</p> <pre><code>ContourHeatmap palette=plasma contour_levels=12\n</code></pre>"},{"location":"reference/analysis-commands.html#backgroundmedian","title":"<code>BackgroundMedian</code>","text":"<p>Computes the sigma-clipped background median for the current frame. This is one of the five metrics <code>AnalyzeFrames</code> computes internally for every frame; running it standalone is useful for inspecting or tuning background estimation on a single frame.</p> <pre><code>BackgroundMedian [sigma=&lt;float&gt;] [iterations=&lt;int&gt;] [grid=&lt;int&gt;]\n</code></pre> Argument Required Default Description <code>sigma</code> No <code>3.0</code> Sigma-clipping threshold in std dev units <code>iterations</code> No <code>5</code> Maximum sigma-clipping iterations <code>grid</code> No <code>4</code> Grid divisions per axis used internally for gradient estimation <pre><code>BackgroundMedian\nBackgroundMedian sigma=2.5 iterations=8\n</code></pre>"},{"location":"reference/display-navigation.html","title":"Display &amp; Navigation","text":"<p>Commands for moving between frames, caching blink thumbnails, and opening the analysis views.</p>"},{"location":"reference/display-navigation.html#setframe","title":"<code>SetFrame</code>","text":"<p>Sets the current active frame by zero-based index.</p> <pre><code>SetFrame index=&lt;integer&gt;\n</code></pre> <pre><code>SetFrame index=0\n</code></pre>"},{"location":"reference/display-navigation.html#cacheframes","title":"<code>CacheFrames</code>","text":"<p>Pre-renders all loaded images to blink-resolution JPEGs, required before using blink playback.</p> <pre><code>CacheFrames [resolution=&lt;12|25&gt;]\n</code></pre> Argument Required Default Description <code>resolution</code> No <code>12</code> (12.5%) or <code>25</code> (25%). If omitted, both resolutions are cached. <pre><code>CacheFrames\nCacheFrames resolution=25\n</code></pre>"},{"location":"reference/display-navigation.html#clearannotations","title":"<code>ClearAnnotations</code>","text":"<p>Removes all star and analysis overlay annotations from the viewer.</p> <pre><code>ClearAnnotations\n</code></pre>"},{"location":"reference/display-navigation.html#showanalysisgraph","title":"<code>ShowAnalysisGraph</code>","text":"<p>Opens the Analysis Graph view.</p> <pre><code>AnalyzeFrames\nShowAnalysisGraph\n</code></pre>"},{"location":"reference/display-navigation.html#showanalysisresults","title":"<code>ShowAnalysisResults</code>","text":"<p>Opens the Analysis Results table view.</p> <pre><code>AnalyzeFrames\nShowAnalysisResults\n</code></pre>"},{"location":"reference/examples.html","title":"Complete Examples","text":"<p>Worked, end-to-end scripts combining commands from the other reference pages.</p>"},{"location":"reference/examples.html#batch-format-conversion-fits-xisf","title":"Batch format conversion: FITS \u2192 XISF","text":"<pre><code># Convert all lights in a directory from FITS to XISF\nClearSession\nReadImages path=\"/data/M31/lights\"\nWriteXISF destination=\"/data/M31/xisf\" overwrite=false compress=false\nPrint \"Conversion complete.\"\n</code></pre>"},{"location":"reference/examples.html#quality-analysis-and-review-workflow","title":"Quality analysis and review workflow","text":"<pre><code># Standard analysis workflow\nClearSession\nReadImages path=\"/data/NGC7331/lights\"\n\nCountFiles\nAssert expression=\"$filecount &gt; 0\"\nPrint \"Loaded \" + $filecount + \" frames\"\n\nAnalyzeFrames\nShowAnalysisResults\n</code></pre> <p>After reviewing results and committing, pass frames remain loaded and are ready to stack.</p>"},{"location":"reference/examples.html#filter-session-by-keyword-then-write","title":"Filter session by keyword then write","text":"<pre><code># Keep only Ha frames, write to a separate directory\nFilterByKeyword name=FILTER value=Ha\nCountFiles\nPrint \"Ha frames: \" + $filecount\nWriteFIT destination=\"/data/Ha-only\" overwrite=true\n</code></pre>"},{"location":"reference/examples.html#per-frame-fwhm-report-with-log","title":"Per-frame FWHM report with log","text":"<pre><code># Measure FWHM on every frame and write results to a log file\nReadImages path=\"/data/lights\"\n\nCountFiles\nFor i = 0 To $filecount - 1\n  SetFrame index=$i\n  GetKeyword name=DATE-OBS\n  ComputeFWHM\n  Print $DATE-OBS + \"  FWHM=\" + $fwhm\nEndFor\n\nLog path=\"/logs/fwhm_report.log\"\n</code></pre>"},{"location":"reference/examples.html#numeric-loop-step-through-frames-by-index","title":"Numeric loop: step through frames by index","text":"<pre><code># Visit the first five frames by index\nFor i = 0 To 4\n  SetFrame index=$i\n  ComputeFWHM\n  Print \"Frame \" + $i + \": FWHM=\" + $fwhm\nEndFor\n</code></pre>"},{"location":"reference/examples.html#conditional-processing-based-on-keyword","title":"Conditional processing based on keyword","text":"<pre><code># Apply different stretch depending on filter\nReadImages path=\"/data/session\"\n\nCountFiles\nFor i = 0 To $filecount - 1\n  SetFrame index=$i\n  GetKeyword name=FILTER\n  If $FILTER == \"Ha\"\n    AutoStretch shadowClip=-2.4 targetBackground=0.10\n  Else\n    AutoStretch shadowClip=-2.8 targetBackground=0.20\n  EndIf\nEndFor\n</code></pre>"},{"location":"reference/examples.html#heatmap-generation-with-file-capture","title":"Heatmap generation with file capture","text":"<pre><code># Generate a contour heatmap and copy it to a review folder\nSetFrame index=0\nContourHeatmap palette=plasma contour_levels=12\nPrint \"Heatmap written to: \" + $NEW_FILE\nCopyFile source=$NEW_FILE destination=\"/data/review\"\n</code></pre>"},{"location":"reference/examples.html#full-stack-pipeline","title":"Full stack pipeline","text":"<pre><code># Load, analyze, stack, stretch, and export\nClearSession\nReadImages path=\"/data/M31/lights\"\nAnalyzeFrames\nShowAnalysisResults\n# After manually committing rejects in the UI, continue:\nStackFrames\nCommitStretch shadow_clip=-3.5 target_bg=0.10\nWriteXISF destination=\"/data/M31/stacked\" stack=true\nPrint \"Stack complete.\"\n</code></pre>"},{"location":"reference/examples.html#calling-a-sub-macro","title":"Calling a sub-macro","text":"<pre><code># Main workflow delegates to reusable sub-macros\nRunMacro name=\"load-and-check\"\nRunMacro name=\"analyze-and-report\"\n</code></pre> <p>This allows building libraries of composable macros pinned to the Quick Launch bar.</p>"},{"location":"reference/image-processing.html","title":"Image Processing","text":"<p>Commands for stretching and debayering frames.</p>"},{"location":"reference/image-processing.html#autostretch","title":"<code>AutoStretch</code>","text":"<p>Applies an automatic stretch to the current frame for display using the PixInsight-compatible Auto-STF algorithm. The raw pixel buffer is not modified.</p> <pre><code>AutoStretch [shadowClip=&lt;float&gt;] [targetBackground=&lt;float&gt;]\n</code></pre> Argument Required Default Description <code>shadowClip</code> No <code>-2.8</code> Shadow clipping point in sigma units <code>targetBackground</code> No <code>0.15</code> Target background level (0.0\u20131.0) <pre><code>AutoStretch shadowClip=-2.8 targetBackground=0.25\n</code></pre>"},{"location":"reference/image-processing.html#debayerimage","title":"<code>DebayerImage</code>","text":"<p>Debayers a Bayer CFA image to interleaved RGB using bilinear interpolation. Operates on the transient stack result if one exists; otherwise operates on the current session frame. The Bayer pattern is always read from the <code>BAYERPAT</code> (or <code>BAYER_PATTERN</code>) keyword, defaulting to RGGB if neither is present \u2014 there is currently no way to override the pattern or interpolation method from pcode.</p> <pre><code>DebayerImage\n</code></pre> <p>Takes no arguments. Frames that are already RGB are left unchanged (reported, not an error).</p> <pre><code>DebayerImage\n</code></pre>"},{"location":"reference/keyword-commands.html","title":"Keyword Commands","text":"<p>Commands for reading and editing FITS header keywords, either on the current frame or across the whole session.</p>"},{"location":"reference/keyword-commands.html#addkeyword","title":"<code>AddKeyword</code>","text":"<p>Adds or replaces a FITS keyword on loaded images.</p> <pre><code>AddKeyword name=&lt;string&gt; value=&lt;string&gt; [comment=&lt;string&gt;] [scope=all|current]\n</code></pre> Argument Required Default Description <code>name</code> Yes Keyword name (max 8 characters) <code>value</code> Yes Keyword value <code>comment</code> No FITS comment <code>scope</code> No <code>all</code> <code>all</code> frames or <code>current</code> only <pre><code>AddKeyword name=TELESCOP value=\"Celestron EdgeHD 8\" comment=\"Telescope used\"\nAddKeyword name=PXFLAG value=PASS scope=current\n</code></pre>"},{"location":"reference/keyword-commands.html#modifykeyword","title":"<code>ModifyKeyword</code>","text":"<p>Changes the value of an existing FITS keyword.</p> <pre><code>ModifyKeyword name=&lt;string&gt; value=&lt;string&gt; [comment=&lt;string&gt;] [scope=all|current]\n</code></pre> Argument Required Default Description <code>name</code> Yes Keyword name to modify <code>value</code> Yes New keyword value <code>comment</code> No New comment <code>scope</code> No <code>all</code> <code>all</code> frames or <code>current</code> only <pre><code>ModifyKeyword name=OBJECT value=\"M31 Andromeda\" scope=all\n</code></pre>"},{"location":"reference/keyword-commands.html#deletekeyword","title":"<code>DeleteKeyword</code>","text":"<p>Removes a FITS keyword from loaded images.</p> <pre><code>DeleteKeyword name=&lt;string&gt; [scope=all|current]\n</code></pre> Argument Required Default Description <code>name</code> Yes Keyword name to delete <code>scope</code> No <code>all</code> <code>all</code> frames or <code>current</code> only <pre><code>DeleteKeyword name=EXPTIME scope=all\n</code></pre>"},{"location":"reference/keyword-commands.html#copykeyword","title":"<code>CopyKeyword</code>","text":"<p>Copies a keyword value from one keyword name to another.</p> <pre><code>CopyKeyword from=&lt;string&gt; to=&lt;string&gt; [scope=all|current]\n</code></pre> Argument Required Default Description <code>from</code> Yes Source keyword name <code>to</code> Yes Destination keyword name <code>scope</code> No <code>all</code> <code>all</code> frames or <code>current</code> only <pre><code>CopyKeyword from=EXPTIME to=EXPOSURE\nCopyKeyword from=EXPTIME to=EXPOSURE scope=current\n</code></pre>"},{"location":"reference/keyword-commands.html#getkeyword","title":"<code>GetKeyword</code>","text":"<p>Retrieves a FITS keyword value from the current frame and stores it in <code>$&lt;NAME&gt;</code> (uppercased). If the keyword is not found and <code>default=</code> is given, the default value is stored instead of halting the script \u2014 useful for optional keywords that may be missing on older or third-party captures.</p> <pre><code>GetKeyword name=&lt;string&gt; [default=&lt;string&gt;]\n</code></pre> Argument Required Description <code>name</code> Yes Keyword name to retrieve <code>default</code> No Fallback value if the keyword is not found on the current frame, instead of halting the script (e.g. <code>default=\"\"</code> or <code>default=\"NULL\"</code>). Does not apply to no-frame-loaded errors. <p>Side effect: Stores result in <code>$&lt;NAME&gt;</code>. For example, <code>GetKeyword name=FILTER</code> stores the value in <code>$FILTER</code>.</p> <pre><code>GetKeyword name=FILTER\nPrint $FILTER\n\nGetKeyword name=OBJECT default=\"\"\nIf $OBJECT == \"\"\n  Print \"OBJECT keyword not set\"\nEndIf\n</code></pre>"},{"location":"reference/keyword-commands.html#listkeywords","title":"<code>ListKeywords</code>","text":"<p>Lists all FITS header keywords for the current frame, sorted alphabetically. Also opens the Keyword Editor panel.</p> <pre><code>ListKeywords\n</code></pre>"},{"location":"reference/language-basics.html","title":"Language Basics","text":"<p>pcode is line-oriented: each line is either a command, a variable assignment, a flow-control statement, or a comment. Macros are saved in the Photyx database and can be run from the console, the Quick Launch bar, or via <code>RunMacro</code>.</p>"},{"location":"reference/language-basics.html#basics","title":"Basics","text":""},{"location":"reference/language-basics.html#comments","title":"Comments","text":"<p>Any line beginning with <code>#</code> is ignored.</p> <pre><code># This is a comment\nAddFiles paths=\"/data/lights/frame001.fit\"   # inline comments are not supported\n</code></pre>"},{"location":"reference/language-basics.html#command-syntax","title":"Command syntax","text":"<pre><code>CommandName arg1=value arg2=\"string value\"\n</code></pre> <p>Arguments are named. Argument names are case-insensitive. String values containing spaces must be enclosed in double quotes. Boolean arguments accept <code>true</code> or <code>false</code>.</p>"},{"location":"reference/language-basics.html#running-a-macro-from-the-console","title":"Running a macro from the console","text":"<p>Type the macro name directly after <code>RunMacro</code>:</p> <pre><code>RunMacro name=\"my-workflow\"\n</code></pre> <p>Or open it in the Macro Editor and click Run.</p>"},{"location":"reference/language-basics.html#variables","title":"Variables","text":"<p>Variables are set with <code>Set</code> and referenced with a <code>$</code> prefix.</p> <pre><code>Set count = 10\nSet label = \"Frame \" + $count\nPrint $label\n</code></pre> <ul> <li>Variable names are case-insensitive when read (<code>$fwhm</code> and <code>$FWHM</code>   refer to the same value).</li> <li>String literals on the right-hand side of <code>Set</code> must use double   quotes.</li> <li>Variables persist for the lifetime of the script execution and are   visible to any macro called via <code>RunMacro</code>.</li> </ul>"},{"location":"reference/language-basics.html#arithmetic","title":"Arithmetic","text":"<p><code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, <code>^</code> (exponentiation) are supported. Parentheses group sub-expressions.</p> <pre><code>Set area   = 3.14159 * $r ^ 2\nSet scaled = ($raw - $min) / ($max - $min)\n</code></pre>"},{"location":"reference/language-basics.html#string-concatenation","title":"String concatenation","text":"<p>The <code>+</code> operator concatenates when either operand is non-numeric.</p> <pre><code>Set path = \"/data/\" + $target + \"/lights\"\n</code></pre>"},{"location":"reference/language-basics.html#math-functions","title":"Math functions","text":"Function Description <code>sqrt(x)</code> Square root <code>abs(x)</code> Absolute value <code>round(x)</code> Round to nearest integer <code>floor(x)</code> Round down <code>ceil(x)</code> Round up <code>min(x, y)</code> Smaller of two values <code>max(x, y)</code> Larger of two values <pre><code>Set sigma = sqrt(($x - $mean) ^ 2)\nSet clipped = min($value, 65535)\n</code></pre>"},{"location":"reference/language-basics.html#path-functions","title":"Path functions","text":"Function Description <code>basename($path)</code> Filename portion of a path, leading directories stripped <code>dirof($path)</code> Directory portion of a path, filename stripped <code>stripext($path)</code> Strips a trailing suffix appended after a known image extension (<code>.fit</code>, <code>.fits</code>, <code>.fts</code>, <code>.xisf</code>) \u2014 e.g. the <code>.session</code>/<code>.project</code> suffix added by <code>CommitAnalysis</code> <pre><code>Set name   = basename($f)\nSet dir    = dirof($f)\nSet parent = dirof(dirof($f))\nSet clean  = stripext($f)\n</code></pre>"},{"location":"reference/language-basics.html#system-set-variables","title":"System-set variables","text":"<p>Several commands automatically store their results in variables.</p> Variable Set by <code>$fwhm</code> <code>ComputeFWHM</code> <code>$eccentricity</code> <code>ComputeEccentricity</code> <code>$starcount</code> <code>CountStars</code> <code>$filecount</code> <code>CountFiles</code> <code>$matchcount</code> <code>CountMatches</code> <code>$STACKED</code> <code>WriteFIT stack=true</code>, <code>WriteXISF stack=true</code> <code>$NEW_FILE</code> <code>ContourHeatmap</code>, <code>CopyFile</code>, <code>MoveFile</code> <code>$LOAD_FILE_PATH</code> <code>LoadFile</code> <code>$&lt;KEYWORDNAME&gt;</code> <code>GetKeyword name=&lt;KEYWORDNAME&gt;</code> (uppercased; falls back to <code>default=</code> if given and the keyword is not found) <code>$&lt;name&gt;</code> <code>GetSystemPath name=&lt;name&gt;</code> (e.g. <code>name=downloads</code> stores <code>$downloads</code>) <p>Example \u2014 reading a keyword into a variable:</p> <pre><code>GetKeyword name=FILTER\nPrint $FILTER\n</code></pre>"},{"location":"reference/language-basics.html#flow-control","title":"Flow Control","text":""},{"location":"reference/language-basics.html#conditionals","title":"Conditionals","text":"<pre><code>If &lt;expression&gt;\n  ...\nElse\n  ...\nEndIf\n</code></pre> <p>The <code>Else</code> branch is optional. <code>If</code> blocks may be nested. Supported comparison operators: <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>. String comparisons are case-insensitive. Equality is always <code>==</code> \u2014 a single <code>=</code> is assignment syntax used by <code>Set</code>, not a valid condition operator.</p> <pre><code>If $fwhm &gt; 3.0\n  Print \"Poor focus \u2014 skipping\"\nElse\n  Print \"Focus acceptable\"\nEndIf\n</code></pre> <pre><code>If $FILTER == \"Ha\"\n  Print \"Narrowband session\"\nEndIf\n</code></pre>"},{"location":"reference/language-basics.html#loops-iterating-over-a-numeric-range","title":"Loops \u2014 iterating over a numeric range","text":"<pre><code>For varname = N To M\n  ...\nEndFor\n</code></pre> <p>The loop variable steps from N to M inclusive. Both bounds can be variables or expressions.</p> <pre><code>Set frames = 10\nFor i = 1 To $frames\n  Print \"Processing frame \" + $i\nEndFor\n</code></pre>"},{"location":"reference/language-basics.html#loops-iterating-over-a-glob-pattern","title":"Loops \u2014 iterating over a glob pattern","text":"<p><code>for &lt;var&gt; in \"&lt;pattern&gt;\"</code> expands a glob pattern and iterates over each matched path, binding it to the loop variable. The variable holds the full matched path as a string. Patterns may include wildcards in any path segment.</p> <pre><code>for &lt;var&gt; in \"&lt;glob_pattern&gt;\"\n  ...\nEndFor\n</code></pre> <pre><code>for d in \"J:/projects/M82/M82-*-sess-*\"\n  Print $d\nEndFor\n</code></pre> <p>Loops may be nested. Numeric and glob loops can be mixed. If a glob pattern matches nothing, a warning is reported and the loop body simply doesn't execute \u2014 the script continues normally rather than halting.</p> <pre><code>for d in \"J:/projects/M82/M82-ircut-sess-*\"\n  ClearSession\n  AddFiles paths=\"$d/lights/*.fit\"\n  AnalyzeFrames profile=\"Broadband\"\n  CommitAnalysis append=.reject\nEndFor\n</code></pre>"},{"location":"reference/language-basics.html#loops-iterating-over-all-session-files","title":"Loops \u2014 iterating over all session files","text":"<p>This is the standard way to process all frames in a session.</p> <pre><code>CountFiles\nFor i = 0 to $filecount - 1\n  SetFrame index=$i\n  ComputeFWHM\n  Print $fwhm\nEndFor\n</code></pre>"},{"location":"reference/language-basics.html#error-handling","title":"Error Handling","text":"<p>By default, pcode halts on the first error. A failed command stops the script and reports the error to the console.</p> <p>Use <code>Assert</code> to add explicit checks:</p> <pre><code>Assert expression=\"$filecount &gt; 0\"\n</code></pre> <p><code>Assert</code> halts execution with an <code>ASSERT_FAILED</code> error if the condition is false. It is silent on pass in both Trace and No Trace modes.</p>"},{"location":"reference/language-basics.html#console-output","title":"Console Output","text":""},{"location":"reference/language-basics.html#print","title":"Print","text":"<p>Outputs an evaluated expression to the console:</p> <pre><code>Print \"Hello world\"\nPrint $fwhm\nPrint \"FWHM: \" + $fwhm\nPrint $x + 1\n</code></pre>"},{"location":"reference/language-basics.html#log","title":"Log","text":"<p>Writes all console output accumulated since the last <code>Log</code> call to a file. Each <code>Log</code> call resets the accumulation point, so multiple <code>Log</code> calls within a single macro can direct different segments of output to different files. Useful for recording analysis results from batch runs.</p> <pre><code>Log path=\"/logs/session.log\"\nLog path=\"/logs/session.log\" append=true\n</code></pre> <pre><code># First segment goes to the FWHM log\nCountFiles\nFor i = 0 to $filecount - 1\n  SetFrame index=$i\n  ComputeFWHM\n  Print $fwhm\nEndFor\nLog path=\"/logs/fwhm.log\"\n\n\n# Second segment goes to the star count log\nCountFiles\nFor i = 0 to $filecount - 1\n  SetFrame index=$i\n  CountStars\n  Print $starcount\nEndFor\nLog path=\"/logs/starcounts.log\"\n</code></pre>"},{"location":"reference/language-basics.html#trace-mode","title":"Trace Mode","text":"<p>The Trace / No Trace toggle in the console header controls verbosity. In Trace mode, each command and its resolved arguments are echoed before execution. In No Trace mode, only output explicitly produced by <code>Print</code> or a command's result message is shown.</p>"},{"location":"reference/scripting-utilities.html","title":"Scripting Utilities","text":"<p>General-purpose commands for variables, output, control flow, and system paths. See Language Basics for the underlying syntax rules these build on.</p>"},{"location":"reference/scripting-utilities.html#set","title":"<code>Set</code>","text":"<p>Assigns a value to a script variable.</p> <pre><code>Set &lt;varname&gt; = &lt;expression&gt;\n</code></pre> <pre><code>Set x = 10\nSet label = \"Frame \" + $x\nSet sigma = sqrt(($x - $mean) ^ 2)\n</code></pre>"},{"location":"reference/scripting-utilities.html#print","title":"<code>Print</code>","text":"<p>Outputs an evaluated expression to the console.</p> <pre><code>Print &lt;expression&gt;\n</code></pre> <pre><code>Print \"Hello world\"\nPrint $fwhm\nPrint \"FWHM: \" + $fwhm\n</code></pre>"},{"location":"reference/scripting-utilities.html#assert","title":"<code>Assert</code>","text":"<p>Halts execution with an error if the condition is false. Silent on pass.</p> <pre><code>Assert expression=&lt;condition&gt;\n</code></pre> <pre><code>Assert expression=\"$filecount &gt; 0\"\nAssert expression=\"$fwhm &lt; 5.0\"\n</code></pre>"},{"location":"reference/scripting-utilities.html#countmatches","title":"<code>CountMatches</code>","text":"<p>Counts filesystem entries (files or directories) matching a glob pattern and stores the result in <code>$matchcount</code>. Useful for conditionally executing a block only when matching entries exist, without loading them into the session.</p> <pre><code>CountMatches pattern=&lt;glob&gt;\n</code></pre> Argument Required Description <code>pattern</code> Yes Glob pattern to match. Supports <code>*</code>, <code>?</code>, and <code>[...]</code> wildcards anywhere in the path. <pre><code>CountMatches pattern=\"$project/*-duo-*\"\nIf $matchcount &gt; 0\n  Print \"Found \" + $matchcount + \" duo sessions\"\nEndIf\n</code></pre>"},{"location":"reference/scripting-utilities.html#getsystempath","title":"<code>GetSystemPath</code>","text":"<p>Retrieves a well-known system directory path and stores it in a variable named after the requested path.</p> <pre><code>GetSystemPath name=&lt;downloads|documents|desktop|temp|home|log|db&gt;\n</code></pre> Argument Required Description <code>name</code> Yes System path to retrieve: <code>downloads</code>, <code>documents</code>, <code>desktop</code>, <code>temp</code>, <code>home</code>, <code>log</code>, or <code>db</code>. Result stored in <code>$&lt;name&gt;</code>. <pre><code>GetSystemPath name=downloads\nPrint $downloads\nExportAnalysisReport path=\"$downloads/M82-Project-Analysis.json\"\n\nGetSystemPath name=home\nPrint $home\n</code></pre>"},{"location":"reference/scripting-utilities.html#runmacro","title":"<code>RunMacro</code>","text":"<p>Executes a saved macro by name from the database. Inner command output and <code>Print</code> statements appear in the console line by line.</p> <pre><code>RunMacro name=&lt;string&gt;\n</code></pre> <pre><code>RunMacro name=\"my-workflow\"\n</code></pre>"},{"location":"reference/scripting-utilities.html#log","title":"<code>Log</code>","text":"<p>Writes all console output accumulated since the last <code>Log</code> call to a file. This means you specify the Log file after the commands whose output you want captured.</p> <pre><code>Log path=&lt;path&gt; [append=&lt;bool&gt;]\n</code></pre> Argument Required Default Description <code>path</code> Yes Output file path <code>append</code> No <code>false</code> Append to existing file instead of erasing <pre><code>Log path=\"/logs/session.log\" append=true\n</code></pre>"},{"location":"reference/scripting-utilities.html#if-else-endif","title":"<code>If</code> / <code>Else</code> / <code>EndIf</code>","text":"<p>Conditional execution. See Flow Control.</p>"},{"location":"reference/scripting-utilities.html#for-endfor","title":"<code>For</code> / <code>EndFor</code>","text":"<p>Two loop forms \u2014 numeric range and glob iterator \u2014 both closed with <code>EndFor</code>. Loops may be nested and mixed.</p> <p>Numeric range: <pre><code>For &lt;var&gt; = N To M\n  ...\nEndFor\n</code></pre></p> <p>Glob iterator: <pre><code>for &lt;var&gt; in \"&lt;glob_pattern&gt;\"\n  ...\nEndFor\n</code></pre></p> <p>See Flow Control for full details and examples.</p>"},{"location":"reference/scripting-utilities.html#console-built-ins","title":"Console Built-ins","text":"<p>These commands are available in the interactive console but have no effect inside a saved macro.</p> Command Description <code>Help</code> Opens help for a specific command, or lists all commands <code>Help &lt;command&gt;</code> Shows syntax and examples for that command <code>Clear</code> Clears the console output buffer <code>Version</code> Prints Photyx and pcode version information <code>pwd</code> Lists unique source directories of all loaded files"},{"location":"reference/session-commands.html","title":"Session Commands","text":"<p>Commands for loading, clearing, and filtering the files in the current session.</p>"},{"location":"reference/session-commands.html#addfiles","title":"<code>AddFiles</code>","text":"<p>Appends one or more files to the current session. Accepts explicit file paths, glob patterns, or a mix of both in a comma-separated list. Files already loaded are skipped. Use <code>ClearSession</code> first to start a fresh session.</p> <pre><code>AddFiles paths=&lt;path|glob&gt;[,&lt;path|glob&gt;...]\n</code></pre> Argument Required Description <code>paths</code> Yes Comma-separated list of file paths and/or glob patterns <p>Glob wildcards: <code>*</code> matches any sequence of characters, <code>?</code> matches a single character, <code>[...]</code> matches a character class. Glob patterns can appear anywhere in the path, including intermediate directory segments. Unmatched patterns produce a warning rather than an error.</p> <pre><code>AddFiles paths=\"/data/M31/frame001.fit,/data/M31/frame002.fit\"\nAddFiles paths=\"/data/M31/lights/*.fit\"\nAddFiles paths=\"J:/projects/M82/M82-*-sess-*/lights/*.fit\"\nAddFiles paths=\"/data/M31/lights/*.fit,/data/M31/extra/frame099.fit\"\n</code></pre>"},{"location":"reference/session-commands.html#readimages","title":"<code>ReadImages</code>","text":"<p>Loads a single image file or all supported images in a directory into the session. Files already loaded are skipped.</p> <pre><code>ReadImages path=&lt;path&gt;\n</code></pre> Argument Required Description <code>path</code> Yes Path to a file or directory <pre><code>ReadImages path=\"/home/stan/lights\"\nReadImages path=\"/home/stan/lights/frame001.xisf\"\n</code></pre>"},{"location":"reference/session-commands.html#clearsession","title":"<code>ClearSession</code>","text":"<p>Clears all files and state from the current session.</p> <pre><code>ClearSession\n</code></pre>"},{"location":"reference/session-commands.html#loadfile","title":"<code>LoadFile</code>","text":"<p>Loads a single file for display, adding it to the session file list (by design \u2014 this is not an isolated preview). Stores the path in <code>$LOAD_FILE_PATH</code>. This command is used from <code>File &gt; Load Single Image</code>.</p> Argument Required Description <code>path</code> Yes Full path to file <pre><code>LoadFile path=\"/data/heatmaps/fwhm_heatmap.xisf\"\n</code></pre>"},{"location":"reference/session-commands.html#countfiles","title":"<code>CountFiles</code>","text":"<p>Stores the number of files currently loaded in the session in <code>$filecount</code>.</p> <pre><code>CountFiles\nPrint $filecount\n</code></pre>"},{"location":"reference/session-commands.html#filterbykeyword","title":"<code>FilterByKeyword</code>","text":"<p>Filters the session file list to only those frames where the specified keyword matches the given value. Non-matching frames are removed from the session.</p> <pre><code>FilterByKeyword name=&lt;string&gt; value=&lt;string&gt;\n</code></pre> Argument Required Description <code>name</code> Yes Keyword name to filter on <code>value</code> Yes Value to match (case-insensitive) <pre><code>FilterByKeyword name=FILTER value=Ha\nFilterByKeyword name=OBJECT value=\"M31\"\n</code></pre>"},{"location":"reference/session-commands.html#rejectframe","title":"<code>RejectFrame</code>","text":"<p>Moves a single frame to a <code>rejected/</code> subfolder within its own source directory, removing it from the session and all caches. Defaults to the current frame if <code>index</code> is omitted.</p> <pre><code>RejectFrame [index=&lt;integer&gt;] [append=&lt;ext&gt;]\n</code></pre> Argument Required Default Description <code>index</code> No current frame Zero-based frame index to reject <code>append</code> No Suffix appended after the original filename extension (e.g. <code>append=cloudy</code> \u2192 <code>frame.fit.cloudy</code>). Leading dot is optional. <p>Unlike <code>CopyFile</code>, <code>MoveFile</code>, and <code>ContourHeatmap</code>, this command does not store its output path in <code>$NEW_FILE</code> \u2014 it has no system-set variable side effect.</p> <pre><code># Reject the current frame\nRejectFrame\n\n# Reject a specific frame by index, with a custom suffix\nRejectFrame index=12 append=cloudy\n</code></pre>"},{"location":"reference/stacking-commands.html","title":"Stacking Commands","text":"<p>Commands for producing, stretching, and clearing the transient stack result.</p>"},{"location":"reference/stacking-commands.html#stackframes","title":"<code>StackFrames</code>","text":"<p>Stacks all session frames into a single result image using meridian-flip-aware group reference selection, FFT phase-correlation + triangle rigid alignment, and two-pass sigma-clipped mean combination. Color-aware: if the reference frame is Bayer or RGB, the stack accumulates all three channels.</p> <pre><code>StackFrames\n</code></pre> <p>Takes no arguments \u2014 calibration is applied separately before frames are loaded into the session, not as part of this command.</p> <pre><code>StackFrames\n</code></pre>"},{"location":"reference/stacking-commands.html#commitstretch","title":"<code>CommitStretch</code>","text":"<p>Permanently applies the Auto-STF stretch to the stack result pixel buffer. After committing, the stack buffer holds non-linear (stretched) data. Use <code>WriteXISF stack=true</code> to export.</p> <pre><code>CommitStretch [shadow_clip=&lt;float&gt;] [target_bg=&lt;float&gt;]\n</code></pre> Argument Required Description <code>shadow_clip</code> No Shadow clipping factor (default: current context value) <code>target_bg</code> No Target background value 0.0\u20131.0 (default: current context value) <pre><code>CommitStretch shadow_clip=-3.5 target_bg=0.10\n</code></pre>"},{"location":"reference/stacking-commands.html#clearstack","title":"<code>ClearStack</code>","text":"<p>Discards the transient stack result and per-frame contribution data, returning the viewer to the normal session image.</p> <pre><code>ClearStack\n</code></pre>"},{"location":"reference/write-export-commands.html","title":"Write &amp; Export Commands","text":"<p>Commands for writing session frames or the transient stack result back to disk, and for copying or moving files.</p>"},{"location":"reference/write-export-commands.html#writecurrent","title":"<code>WriteCurrent</code>","text":"<p>Writes all buffered images back to their source paths. For <code>.fit</code>/<code>.fits</code>/<code>.fts</code> files this rewrites keywords only \u2014 the pixel data on disk is untouched, which makes this the standard way to persist keyword changes across a whole session without a full rewrite. For <code>.xisf</code> files it performs a full rewrite (pixels and keywords together), since that format doesn't support in-place keyword patching. Uses an atomic temp-rename for XISF.</p> <pre><code>WriteCurrent\n</code></pre>"},{"location":"reference/write-export-commands.html#writeframe","title":"<code>WriteFrame</code>","text":"<p>Writes the currently active frame only back to its source path, using an atomic temp-rename. Unlike <code>WriteCurrent</code>, this always performs a full pixel + keyword rewrite regardless of format \u2014 including <code>.fit</code> files.</p> <pre><code>WriteFrame\n</code></pre>"},{"location":"reference/write-export-commands.html#writefit","title":"<code>WriteFIT</code>","text":"<p>Writes all session files to a destination directory in FITS format. Use <code>stack=true</code> to write the transient stack result as a single file. The <code>.fit</code> extension is appended automatically for session-frame output. When <code>stack=true</code>, stores the output path in <code>$STACKED</code>.</p> <pre><code>WriteFIT destination=&lt;path&gt; [overwrite=&lt;bool&gt;] [stack=&lt;bool&gt;]\n</code></pre> Argument Required Default Description <code>destination</code> Yes Output directory (session frames) or file path (stack=true) <code>overwrite</code> No <code>false</code> Overwrite existing files <code>stack</code> No <code>false</code> Write the transient stack result as a single FITS file instead of all session frames <pre><code>WriteFIT destination=\"/data/output\" overwrite=true\nWriteFIT destination=\"/data/masters/flat_master\" stack=true\nPrint $STACKED\n</code></pre>"},{"location":"reference/write-export-commands.html#writexisf","title":"<code>WriteXISF</code>","text":"<p>Writes all session files to a destination directory in XISF format. Use <code>stack=true</code> to export the transient stack result instead, using the auto-derived filename pattern <code>Photyx_stack_OBJECT_FILTER_INTEGRATIONTIME_DTG.xisf</code> (e.g. <code>Photyx_stack_M64_ircut_24000s_20260528113121Z.xisf</code>). When <code>stack=true</code>, stores the output path in <code>$STACKED</code>.</p> <pre><code>WriteXISF destination=&lt;path&gt; [overwrite=&lt;bool&gt;] [compress=&lt;bool&gt;] [stack=&lt;bool&gt;]\n</code></pre> Argument Required Default Description <code>destination</code> Yes Directory to write files to <code>overwrite</code> No <code>false</code> Overwrite existing files <code>compress</code> No <code>false</code> Apply LZ4HC compression with byte shuffling <code>stack</code> No <code>false</code> Write the transient stack result instead of frames <pre><code>WriteXISF destination=\"/data/output\" overwrite=true compress=false\nWriteXISF destination=\"/data/output\" stack=true\nPrint $STACKED\n</code></pre>"},{"location":"reference/write-export-commands.html#copyfile","title":"<code>CopyFile</code>","text":"<p>Copies a file to a destination directory. Uses the current frame if no source is specified. Stores the destination path in <code>$NEW_FILE</code>. The source file and session are unchanged. Fails with an error if a file already exists at the destination, unless <code>overwrite=true</code>.</p> <pre><code>CopyFile destination=&lt;path&gt; [source=&lt;path&gt;] [overwrite=&lt;bool&gt;]\n</code></pre> Argument Required Default Description <code>destination</code> Yes Destination directory path (created automatically if needed) <code>source</code> No Source file path (default: current frame) <code>overwrite</code> No <code>false</code> Overwrite an existing file at the destination <p>For example, to back up every frame in the session before processing:</p> <pre><code>CountFiles\nFor i = 0 To $filecount - 1\n  SetFrame index=$i\n  CopyFile destination=\"/data/Backups\" overwrite=true\nEndFor\n</code></pre>"},{"location":"reference/write-export-commands.html#movefile","title":"<code>MoveFile</code>","text":"<p>Moves a file to a destination. Uses the current frame if no source is specified. If the destination is an existing directory (or ends with a path separator), the file is moved into it preserving its filename. Otherwise the destination is treated as a full file path, allowing rename-during-move (<code>mv</code> semantics). The destination parent directory is created automatically if needed. Stores the destination path in <code>$NEW_FILE</code>. Removes the file from the session file list if it was a session file. Fails with an error if a file already exists at the destination, unless <code>overwrite=true</code>. Cross-filesystem moves (e.g. external drive to local disk) use an atomic copy-to-temp-then-rename sequence, so an interrupted move never leaves a partial file at the destination name.</p> <pre><code>MoveFile destination=&lt;path&gt; [source=&lt;path&gt;] [overwrite=&lt;bool&gt;]\n</code></pre> Argument Required Default Description <code>destination</code> Yes Destination directory path, or full destination file path for rename-during-move <code>source</code> No Source file path (default: current frame). May be a file outside the session <code>overwrite</code> No <code>false</code> Overwrite an existing file at the destination <pre><code>MoveFile destination=\"/data/Rejects\"\nMoveFile source=\"$f\" destination=\"/data/Rejects\"\nMoveFile source=\"$f\" destination=\"/data/Rejects\" overwrite=true\n# Rename during move (mv semantics):\nSet cleaned = stripext($f)\nMoveFile source=\"$f\" destination=\"$cleaned\"\n</code></pre>"}]}